using UnityEngine;

public interface IInteractiveButton
{
    // no need to specify public for methods 
    // the interface is already public so all methods are public by default
    void ButtonClick();
}